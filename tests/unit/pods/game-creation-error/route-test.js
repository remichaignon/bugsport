import { moduleFor, test } from "ember-qunit";

moduleFor("route:game-creation-error", "Unit | Route | game creation error");

test("it exists", function(assert) {
  assert.expect(1);

  var route = this.subject();
  assert.ok(route);
});
